from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='practice deploy ml models in production way',
    author='Daillek',
    license='',
)
